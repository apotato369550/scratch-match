● Bones laid. Here's what was added:                       
                                                                                          
  Library
  - src/lib/db.ts — better-sqlite3 client; opens data/app.db, sets WAL + FK pragmas, runs the full schema from DESIGN.md §7 on first import (idempotent CREATE TABLE IF NOT   
  EXISTS).
  - src/lib/password.ts — bcrypt hash/verify (Node-safe; scripts import from here).                                                                                           
  - src/lib/auth.ts — session token gen, sm_session httpOnly cookie, getCurrentUser(), requireRole(). Imports next/headers, so server-only.                                 

  API routes (src/app/api/...)
  - POST /auth/signup (zod-validated; rejects duplicate email; auto-logs in)
  - POST /auth/login / POST /auth/logout
  - GET /me

  Forms wired
  - /login and /signup are now real client forms — submit via fetch, show errors, redirect by role (/admin, /seeker/dashboard, /employer/dashboard, or onboarding).

  Scripts (run via tsx)
  - npm run db:migrate — touches the DB so the schema applies, lists tables.
  - npm run seed:admin — creates/refreshes admin from ADMIN_EMAIL/ADMIN_PASSWORD.
  - npm run ingest:sample — 3 seekers (+ 2 sample CV rows), 2 employers (+ 4 jobs), all idempotent. Relational rows only — embeddings/Qdrant come next milestone.
  - npm run reset — deletes app.db + WAL/SHM.

  Other
  - .env.example per design doc.
  - .gitignore extended with -wal/-shm.
  - package.json adds better-sqlite3, bcryptjs, zod, dotenv, tsx (+ @types/*).

  To run end-to-end (your install):
  npm install
  cp .env.example .env
  npm run db:migrate && npm run seed:admin && npm run ingest:sample
  npm run dev
  Then /signup → onboarding, or /login with admin@scratchmatch.local / changeme, or any *@example.ph / password123.

  Deferred (intentionally — keeping this slice tight): onboarding-form persistence (profile API routes + wiring), dashboards reading real DB data, route-level auth guards.
  Suggest branch feat/db-and-auth for this slice. Want me to commit + merge it the same way as before?